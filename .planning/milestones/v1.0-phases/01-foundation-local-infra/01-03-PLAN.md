---
phase: 01-foundation-local-infra
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/config/package.json
  - packages/config/tsconfig.json
  - packages/config/src/env.ts
  - packages/config/src/index.ts
  - packages/queue/package.json
  - packages/queue/tsconfig.json
  - packages/queue/src/index.ts
  - .env.example
autonomous: true
requirements:
  - QUE-01
must_haves:
  truths:
    - "@omnisync/config exports a Zod-validated env object that fails fast (process.exit non-zero) when required env vars are missing/invalid"
    - ".env.example documents DATABASE_URL, DIRECT_URL, REDIS_URL"
    - "@omnisync/queue (QUE-01) establishes a BullMQ Queue named 'events' over a Redis (ioredis) connection with maxRetriesPerRequest: null"
    - "The BullMQ interval config constants equal guardInterval 30000, stalledInterval 300000, drainDelay 30"
  artifacts:
    - path: "packages/config/src/env.ts"
      provides: "Zod-validated, fail-fast environment loader shared by all apps"
      contains: "safeParse"
    - path: "packages/queue/src/index.ts"
      provides: "Shared BullMQ events queue + Redis connection + locked interval config (QUE-01)"
      contains: "new Queue"
    - path: ".env.example"
      provides: "Documented env contract for all apps"
      contains: "REDIS_URL"
  key_links:
    - from: "packages/queue/src/index.ts"
      to: "bullmq"
      via: "import { Queue } from \"bullmq\""
      pattern: "bullmq"
    - from: "packages/config/src/env.ts"
      to: "zod"
      via: "import { z } from \"zod\""
      pattern: "zod"
---

<objective>
Build the runtime substrate: `@omnisync/config` (a Zod-validated, fail-fast env loader + `.env.example`) and `@omnisync/queue` (the shared BullMQ `events` queue connection with the locked interval config — delivering requirement QUE-01).

Decoupled from Plan 02 (different packages, no shared files) → runs in parallel in Wave 2. Depends only on Plan 01 (workspace tooling + running Redis).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-local-infra/01-CONTEXT.md
@.planning/phases/01-foundation-local-infra/01-RESEARCH.md
@.planning/phases/01-foundation-local-infra/01-VALIDATION.md
@CLAUDE.md

<interfaces>
Exact code is in 01-RESEARCH.md sections 4 (BullMQ queue module) and 5 (Zod env loader + .env.example). ⚠ VERIFY flags there: Zod 4 import subpath + error-formatting API. Confirm against the installed Zod 4 and adjust. The interval constants (guardInterval 30000, stalledInterval 300000, drainDelay 30) are LOCKED by CONTEXT D-10 — do not change the values.
This plan creates the canonical `.env.example`; Plan 02 may have created a temporary local `.env` — keep both consistent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: @omnisync/config — Zod-validated fail-fast env loader + .env.example</name>
  <read_first>
    - 01-RESEARCH.md (section 5)
    - 01-CONTEXT.md (D-13 — shared validated env loader)
    - tsconfig.base.json (Plan 01)
  </read_first>
  <action>
Create `packages/config`:
1. **`package.json`** — name `@omnisync/config`, `"type":"module"`, ESM exports → `./dist/index.js`, scripts `build: tsc`, `typecheck: tsc --noEmit`; dependency `zod` (v4).
2. **`tsconfig.json`** — extends base; outDir dist, rootDir src, include ["src"].
3. **`src/env.ts`** — per RESEARCH.md §5: define a Zod object schema (`NODE_ENV` enum default development; `DATABASE_URL`, `DIRECT_URL`, `REDIS_URL` as `z.string().url()`), `safeParse(process.env)`, and on failure `console.error` the formatted issues + `process.exit(1)`. Export the parsed `env`. Use the verified Zod 4 import + error API.
4. **`src/index.ts`** — `export * from "./env.js";`
5. **`.env.example`** (repo root) — per RESEARCH.md §5: `NODE_ENV`, `DATABASE_URL`, `DIRECT_URL` (both local docker Postgres), `REDIS_URL=redis://localhost:6379`.
Run `pnpm --filter @omnisync/config build`.
  </action>
  <verify>
    <automated>pnpm --filter @omnisync/config build && DATABASE_URL=postgresql://x:y@localhost:5432/z DIRECT_URL=postgresql://x:y@localhost:5432/z REDIS_URL=redis://localhost:6379 node -e "import('@omnisync/config').then(m=>{if(!m.env.REDIS_URL)process.exit(1);console.log('ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'safeParse' packages/config/src/env.ts` succeeds
    - `grep -q 'process.exit(1)' packages/config/src/env.ts` succeeds
    - `test -f .env.example && grep -q 'REDIS_URL' .env.example && grep -q 'DIRECT_URL' .env.example` succeeds
    - `pnpm --filter @omnisync/config build` exits 0
    - With all 3 URLs set, importing `@omnisync/config` exposes `env.REDIS_URL` (node check exits 0)
    - With REDIS_URL unset, importing `@omnisync/config` exits non-zero (fail-fast): `node -e "import('@omnisync/config')"` without REDIS_URL returns non-zero
  </acceptance_criteria>
  <done>
    Shared env loader validates and fails fast on missing config; .env.example documents the contract.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: @omnisync/queue — BullMQ events queue (QUE-01)</name>
  <read_first>
    - 01-RESEARCH.md (section 4 — BullMQ connection + locked config)
    - 01-CONTEXT.md (D-10, D-11 — locked intervals, noeviction)
    - 01-VALIDATION.md (QUE-01 row)
  </read_first>
  <action>
Create `packages/queue`:
1. **`package.json`** — name `@omnisync/queue`, `"type":"module"`, ESM exports → `./dist/index.js`, scripts `build: tsc`, `typecheck: tsc --noEmit`; deps `bullmq` (^5.77), `ioredis`.
2. **`tsconfig.json`** — extends base.
3. **`src/index.ts`** — per RESEARCH.md §4: create an ioredis `connection` from `process.env.REDIS_URL` with `maxRetriesPerRequest: null`; export `QUEUE_NAME = "events"`, a frozen `queueOptions` constant `{ guardInterval: 30_000, stalledInterval: 300_000, drainDelay: 30 }`, and `eventsQueue = new Queue(QUEUE_NAME, { connection })`. No Worker yet (Phase 3).
Run `pnpm --filter @omnisync/queue build`.
  </action>
  <verify>
    <automated>pnpm --filter @omnisync/queue build && REDIS_URL=redis://localhost:6379 node -e "import('@omnisync/queue').then(m=>{if(m.QUEUE_NAME!=='events')process.exit(1);if(m.queueOptions.guardInterval!==30000||m.queueOptions.stalledInterval!==300000||m.queueOptions.drainDelay!==30)process.exit(1);console.log('ok');process.exit(0)})"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'from "bullmq"' packages/queue/src/index.ts` succeeds
    - `grep -q 'new Queue' packages/queue/src/index.ts` succeeds
    - `grep -q 'maxRetriesPerRequest: null' packages/queue/src/index.ts` succeeds
    - `grep -Eq 'guardInterval: 30_?000' packages/queue/src/index.ts` succeeds
    - `grep -Eq 'stalledInterval: 300_?000' packages/queue/src/index.ts` succeeds
    - `grep -q 'drainDelay: 30' packages/queue/src/index.ts` succeeds
    - With Redis up + REDIS_URL set, the node check exits 0 (QUEUE_NAME === "events" and the 3 interval constants match exactly)
  </acceptance_criteria>
  <done>
    QUE-01 satisfied: a shared Redis-backed BullMQ 'events' queue with the locked interval config is established (connection-only).
  </done>
</task>

</tasks>

<verification>
Full plan completion:
```bash
pnpm --filter @omnisync/config build && pnpm --filter @omnisync/queue build && \
REDIS_URL=redis://localhost:6379 node -e "import('@omnisync/queue').then(m=>process.exit(m.QUEUE_NAME==='events'?0:1))"
```
Expected: both packages build; queue exports the 'events' queue with locked config.
</verification>

<success_criteria>
- @omnisync/config validates env and fails fast; .env.example present (DATABASE_URL/DIRECT_URL/REDIS_URL).
- @omnisync/queue establishes the BullMQ 'events' queue with maxRetriesPerRequest:null and locked intervals (QUE-01).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-local-infra/01-03-SUMMARY.md`: files created, verified Zod 4 import/error API used, BullMQ version installed, and confirmation the queue config constants match 30000/300000/30.
</output>
