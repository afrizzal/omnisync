---
phase: 01-foundation-local-infra
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/index.ts
  - apps/api/Dockerfile
  - apps/worker/package.json
  - apps/worker/tsconfig.json
  - apps/worker/src/index.ts
  - apps/worker/Dockerfile
  - apps/dashboard/package.json
  - apps/dashboard/tsconfig.json
  - apps/dashboard/next.config.js
  - apps/dashboard/app/layout.tsx
  - apps/dashboard/app/page.tsx
  - docker-compose.yml
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - OPS-02
must_haves:
  truths:
    - "apps/api, apps/worker, apps/dashboard exist as minimal buildable stubs importing @omnisync/db and/or @omnisync/types with no business logic"
    - "pnpm -r build compiles all packages AND all three apps with no TypeScript errors"
    - "apps/api and apps/worker each have a multi-stage Dockerfile and are wired into docker-compose (OPS-02)"
    - "docker compose build api worker succeeds and docker compose up runs both processes which stay alive"
    - "GitHub Actions CI runs install -> typecheck -> build -> biome check on push (Node 22)"
    - "No circular dependency exists between packages (apps depend on packages, packages do not depend on apps)"
  artifacts:
    - path: "apps/worker/Dockerfile"
      provides: "Multi-stage Docker image for the always-on worker (OPS-02)"
      contains: "node:22"
    - path: "docker-compose.yml"
      provides: "Local compose wiring api + worker images alongside postgres + redis"
      contains: "dockerfile: apps/api/Dockerfile"
    - path: ".github/workflows/ci.yml"
      provides: "CI skeleton: typecheck + build + biome on push"
      contains: "pnpm"
    - path: "apps/dashboard/app/page.tsx"
      provides: "Bare Next.js App Router page importing @omnisync/types (build-time cross-package proof)"
      contains: "@omnisync/types"
  key_links:
    - from: "apps/api/src/index.ts"
      to: "@omnisync/db"
      via: "import { prisma } from \"@omnisync/db\""
      pattern: "@omnisync/db"
    - from: "apps/worker/src/index.ts"
      to: "@omnisync/queue"
      via: "import { QUEUE_NAME } from \"@omnisync/queue\""
      pattern: "@omnisync/queue"
    - from: "docker-compose.yml"
      to: "apps/worker/Dockerfile"
      via: "build.dockerfile"
      pattern: "apps/worker/Dockerfile"
---

<objective>
Scaffold the three app stubs (api, worker, dashboard) that import the shared packages and build cleanly, containerize api + worker with multi-stage Dockerfiles wired into docker-compose (delivering OPS-02), and add the GitHub Actions CI skeleton (Success Criterion #5). Completes Success Criterion #4 (cross-package imports across all three apps, no cycles).

Depends on Plans 02 (@omnisync/db, @omnisync/types) and 03 (@omnisync/config, @omnisync/queue).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-local-infra/01-CONTEXT.md
@.planning/phases/01-foundation-local-infra/01-RESEARCH.md
@.planning/phases/01-foundation-local-infra/01-VALIDATION.md
@CLAUDE.md

<interfaces>
Exact patterns are in 01-RESEARCH.md sections 8 (multi-stage Dockerfiles + compose wiring), 9 (Next.js dashboard stub), 10 (GitHub Actions CI). ⚠ VERIFY flags there: `turbo prune --docker` flags for the installed Turbo major, and current Next.js/React majors. Do NOT add business logic — these are buildable stubs only (CONTEXT D-04, D-05). mock-crm and /healthz are deferred (CONTEXT D-06) — do not add them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: api + worker stubs</name>
  <read_first>
    - 01-RESEARCH.md (section 8 intro — what the stub index.ts should do)
    - packages/db/src/index.ts (Plan 02 — prisma export)
    - packages/queue/src/index.ts (Plan 03 — QUEUE_NAME export)
    - 01-CONTEXT.md (D-04, D-06)
  </read_first>
  <action>
Create `apps/api` and `apps/worker`:
1. Each **`package.json`** — names `@omnisync/api` / `@omnisync/worker`, `"type":"module"`, scripts `build: tsc`, `typecheck: tsc --noEmit`, `start: node dist/index.js`, `dev: tsx watch src/index.ts`. deps: `@omnisync/db: workspace:*`, `@omnisync/types: workspace:*`, `@omnisync/config: workspace:*`, `@omnisync/queue: workspace:*` (worker), `tsx` devDep.
2. Each **`tsconfig.json`** — extends base; outDir dist, rootDir src.
3. **`apps/api/src/index.ts`** — minimal: import `{ prisma }` from `@omnisync/db` and `{ env }` from `@omnisync/config`, log a "api ready" line (referencing env so the import is used). No HTTP server logic required yet (Fastify lands in Phase 2). Keep it a process that exits cleanly OR a 1-line console log — buildable stub.
4. **`apps/worker/src/index.ts`** — minimal: import `{ QUEUE_NAME }` from `@omnisync/queue` and `{ env }` from `@omnisync/config`, log `worker ready for queue ${QUEUE_NAME}`, then keep the process alive (e.g. `setInterval(()=>{}, 1<<30)`) so it stays up under docker-compose. No BullMQ Worker yet (Phase 3).
Use `.js` extensions on relative imports (NodeNext). Build both.
  </action>
  <verify>
    <automated>pnpm --filter @omnisync/api build && pnpm --filter @omnisync/worker build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '@omnisync/db' apps/api/src/index.ts` succeeds
    - `grep -q '@omnisync/queue' apps/worker/src/index.ts` succeeds
    - `apps/api/src/index.ts` contains no Fastify import (`grep -q 'fastify' apps/api/src/index.ts` returns non-zero — deferred to Phase 2)
    - `pnpm --filter @omnisync/api build` and `pnpm --filter @omnisync/worker build` both exit 0, producing `dist/index.js` in each
  </acceptance_criteria>
  <done>
    api + worker stubs build and import shared packages; worker stays alive; no business logic.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Next.js dashboard stub</name>
  <read_first>
    - 01-RESEARCH.md (section 9)
    - packages/types/src/index.ts (Plan 02 — type to import)
    - 01-CONTEXT.md (D-05 — bare Next.js, no real UI)
  </read_first>
  <action>
Create `apps/dashboard` (bare Next.js App Router):
1. **`package.json`** — name `@omnisync/dashboard`, scripts `build: next build`, `dev: next dev`, `typecheck: tsc --noEmit`; deps `next` + `react` + `react-dom` (current majors), `@omnisync/types: workspace:*`.
2. **`tsconfig.json`** — Next-compatible (`module esnext`, `moduleResolution bundler`, `jsx preserve`, `noEmit true`), can extend base where compatible.
3. **`next.config.js`** — `transpilePackages: ["@omnisync/types"]`.
4. **`app/layout.tsx`** — minimal root layout.
5. **`app/page.tsx`** — placeholder that imports a type from `@omnisync/types` (e.g. `import type { InboundEvent } from "@omnisync/types"`) to prove cross-package resolution at build, renders an "OmniSync — coming soon" placeholder. No real dashboard UI (Phase 5).
Run `pnpm --filter @omnisync/dashboard build`.
  </action>
  <verify>
    <automated>pnpm --filter @omnisync/dashboard build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '@omnisync/types' apps/dashboard/app/page.tsx` succeeds
    - `grep -q 'transpilePackages' apps/dashboard/next.config.js` succeeds
    - `pnpm --filter @omnisync/dashboard build` exits 0 (produces `.next/`)
  </acceptance_criteria>
  <done>
    Dashboard builds as a bare Next.js app importing shared types (no real UI yet).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Multi-stage Dockerfiles + compose wiring (OPS-02)</name>
  <read_first>
    - 01-RESEARCH.md (section 8 — Dockerfile + compose service blocks)
    - docker-compose.yml (Plan 01 — append api + worker services, keep postgres/redis)
    - apps/api/src/index.ts, apps/worker/src/index.ts (Task 1 — entry paths)
  </read_first>
  <action>
1. **`apps/api/Dockerfile`** and **`apps/worker/Dockerfile`** — multi-stage per RESEARCH.md §8: `node:22-slim` prune stage (`turbo prune @omnisync/api --docker`), install (`pnpm install --frozen-lockfile`), build (`pnpm --filter @omnisync/<app> build`), runtime stage running `node apps/<app>/dist/index.js`. Worker Dockerfile is identical with the filter swapped to `@omnisync/worker`. Verify the `turbo prune --docker` flags against the installed Turbo.
2. **`docker-compose.yml`** — append `api` and `worker` services per RESEARCH.md §8: `build.context: .`, `build.dockerfile: apps/<app>/Dockerfile`, `env_file: .env`, `depends_on` postgres+redis with `condition: service_healthy`; api exposes a port. Keep the existing postgres + redis services from Plan 01 unchanged.
  </action>
  <verify>
    <automated>docker compose config && docker compose build api worker</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/api/Dockerfile && grep -q 'node:22' apps/api/Dockerfile` succeeds
    - `test -f apps/worker/Dockerfile && grep -q 'node:22' apps/worker/Dockerfile` succeeds
    - `grep -q 'dockerfile: apps/api/Dockerfile' docker-compose.yml` succeeds
    - `grep -q 'dockerfile: apps/worker/Dockerfile' docker-compose.yml` succeeds
    - `docker compose config` exits 0
    - `docker compose build api worker` exits 0 (both images build) — OPS-02
  </acceptance_criteria>
  <done>
    api + worker build as multi-stage Docker images and run via docker-compose locally (OPS-02).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: GitHub Actions CI skeleton</name>
  <read_first>
    - 01-RESEARCH.md (section 10 — ci.yml)
    - 01-VALIDATION.md (SC5 row)
    - package.json (Plan 01 — script names)
  </read_first>
  <action>
Create **`.github/workflows/ci.yml`** per RESEARCH.md §10: trigger on push + pull_request; job `verify` on ubuntu-latest; steps: checkout@v4, pnpm/action-setup@v4, setup-node@v4 (node 22, cache pnpm), `pnpm install --frozen-lockfile`, `pnpm --filter @omnisync/db generate` (prisma client needed before typecheck), `pnpm typecheck`, `pnpm build`, `pnpm lint`. No test job yet (Vitest + ≥80% gate are Phase 6).
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); ['pnpm','typecheck','build','node-version'].forEach(k=>{if(!y.includes(k)){console.error('missing '+k);process.exit(1)}}); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/ci.yml` succeeds
    - `grep -q 'pnpm/action-setup' .github/workflows/ci.yml` succeeds
    - `grep -q 'node-version: 22' .github/workflows/ci.yml` succeeds
    - `grep -q 'pnpm typecheck' .github/workflows/ci.yml && grep -q 'pnpm build' .github/workflows/ci.yml` succeeds
    - `pnpm typecheck && pnpm build && pnpm lint` all exit 0 locally (proves the CI steps will pass green — SC5)
  </acceptance_criteria>
  <done>
    CI skeleton runs typecheck/build/lint on Node 22; the same commands pass locally so CI will be green (SC5).
  </done>
</task>

</tasks>

<verification>
Full plan completion (and full Phase 1 acceptance):
```bash
pnpm install --frozen-lockfile=false && pnpm build && pnpm typecheck && pnpm lint && \
docker compose up -d && pnpm assert:redis && \
pnpm --filter @omnisync/db exec prisma migrate deploy && \
psql "postgresql://omnisync:omnisync@localhost:5432/omnisync" -c "\d events" | grep -q events_fingerprint_unique && \
docker compose build api worker
```
Expected: monorepo builds (all packages + 3 apps), biome clean, infra up, constraint present, api+worker images build.
</verification>

<success_criteria>
- Three app stubs build importing shared packages; `pnpm -r build` green (SC2, SC4).
- api + worker containerized via multi-stage Dockerfiles + compose (OPS-02).
- CI skeleton present; typecheck/build/lint pass (SC5).
- No circular package deps.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-local-infra/01-04-SUMMARY.md`: files created, Next.js/React + Turbo versions used, any ⚠ VERIFY Dockerfile/turbo-prune adjustments, and confirmation that `pnpm build`, `docker compose build api worker`, and the full Phase 1 acceptance command all pass.
</output>
