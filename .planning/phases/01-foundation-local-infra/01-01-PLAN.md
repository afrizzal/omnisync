---
phase: 01-foundation-local-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - .npmrc
  - .nvmrc
  - turbo.json
  - tsconfig.base.json
  - biome.json
  - .gitignore
  - docker-compose.yml
  - scripts/assert-redis.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Repo root is an ESM pnpm + Turborepo workspace: package.json has \"type\":\"module\", \"private\":true, engines.node >=22, and a packageManager pnpm field"
    - "pnpm-workspace.yaml globs apps/* and packages/*"
    - "turbo.json defines build, typecheck, lint tasks (build dependsOn ^build)"
    - "tsconfig.base.json uses module/moduleResolution NodeNext and strict true"
    - "biome.json present and `pnpm exec biome check .` runs without crashing"
    - "docker-compose.yml starts postgres:16 and redis:7 with redis launched using --maxmemory-policy noeviction"
    - "scripts/assert-redis.ts exits 0 only when Redis maxmemory-policy == noeviction (exits 1 otherwise)"
  artifacts:
    - path: "package.json"
      provides: "ESM root workspace manifest + scripts delegating to turbo + biome"
      contains: "\"type\": \"module\""
    - path: "turbo.json"
      provides: "Turborepo task pipeline"
      contains: "\"build\""
    - path: "tsconfig.base.json"
      provides: "Shared strict ESM TS base config extended by all packages/apps"
      contains: "NodeNext"
    - path: "docker-compose.yml"
      provides: "Local Postgres 16 + Redis 7 (noeviction) infra"
      contains: "noeviction"
    - path: "scripts/assert-redis.ts"
      provides: "Boot/CI assertion that Redis is configured noeviction"
      contains: "maxmemory-policy"
  key_links:
    - from: "scripts/assert-redis.ts"
      to: "ioredis"
      via: "import IORedis from \"ioredis\""
      pattern: "ioredis"
    - from: "package.json"
      to: "turbo"
      via: "turbo run"
      pattern: "turbo run"
---

<objective>
Establish the repo as an ESM-native pnpm + Turborepo monorepo with shared tooling (base tsconfig, Biome) AND the local Docker infra (Postgres 16 + Redis 7 with noeviction) that every later plan/phase depends on.

Output:
- Root workspace manifest + Turborepo pipeline + shared base tsconfig + Biome config
- docker-compose.yml bringing up Postgres + Redis (noeviction) locally
- An assert-redis script that fails the boot/CI if Redis is not noeviction (Phase 1 Success Criterion #1)

No application or package code yet — this is the substrate. apps/* and packages/* are created by later plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-local-infra/01-CONTEXT.md
@.planning/phases/01-foundation-local-infra/01-RESEARCH.md
@.planning/phases/01-foundation-local-infra/01-VALIDATION.md
@CLAUDE.md

<interfaces>
Exact, current config snippets to use are in 01-RESEARCH.md sections 1 (monorepo), 6 (docker-compose + redis assert), 7 (Biome). Version-sensitive lines are flagged ⚠ VERIFY there — confirm against installed package versions (`pnpm -v`, `pnpm exec turbo --version`, `pnpm exec biome --version`) and adjust the exact syntax if the installed major differs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Root workspace manifest + ESM + pnpm</name>
  <read_first>
    - 01-RESEARCH.md (section 1 — root package.json, pnpm-workspace.yaml, tsconfig.base.json)
    - 01-CONTEXT.md (D-01..D-03, D-12: Turborepo+pnpm, ESM, Node 22, Biome)
  </read_first>
  <action>
Create the following at repo root:

1. **`package.json`** — ESM, private, Node 22, pnpm packageManager, scripts delegating to Turborepo + Biome. Use the structure from RESEARCH.md §1. Required exact keys/values: `"type": "module"`, `"private": true`, `"engines": { "node": ">=22" }`, `"packageManager": "pnpm@9..."` (set to the installed pnpm version from `pnpm -v`). Scripts: `build: turbo run build`, `typecheck: turbo run typecheck`, `lint: biome check .`, `format: biome format --write .`, `dev: turbo run dev`. devDependencies: `turbo`, `@biomejs/biome`, `typescript`, `ioredis`, `tsx` (tsx to run scripts/assert-redis.ts). Install latest majors with `pnpm add -D -w turbo @biomejs/biome typescript ioredis tsx`.

2. **`pnpm-workspace.yaml`**:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

3. **`.npmrc`** (repo root): add `link-workspace-packages=true` and `prefer-workspace-packages=true` so `workspace:*` deps resolve.

4. **`.nvmrc`**: `22`

Run `pnpm install` to generate `pnpm-lock.yaml`.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false && pnpm exec turbo --version && pnpm exec biome --version</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"type": "module"' package.json` succeeds
    - `grep -q '"private": true' package.json` succeeds
    - `grep -Eq '"packageManager": "pnpm@' package.json` succeeds
    - `grep -q 'turbo run build' package.json` succeeds
    - `test -f pnpm-workspace.yaml && grep -q 'packages/\*' pnpm-workspace.yaml` succeeds
    - `test -f .nvmrc && grep -q '22' .nvmrc` succeeds
    - `pnpm exec turbo --version` exits 0 and prints a version
    - `pnpm exec biome --version` exits 0 and prints a version
  </acceptance_criteria>
  <done>
    Root is an installable ESM pnpm workspace with turbo + biome available.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Turborepo pipeline + shared base tsconfig + Biome</name>
  <read_first>
    - 01-RESEARCH.md (section 1 turbo.json + tsconfig.base.json; section 7 biome.json)
    - package.json (created in Task 1)
  </read_first>
  <action>
Create at repo root:

1. **`turbo.json`** per RESEARCH.md §1. Must define `tasks` (Turbo 2 key) with `build` (`dependsOn: ["^build"]`, outputs `dist/**`, `.next/**`, `generated/**`), `typecheck` (`dependsOn: ["^build"]`), `lint` ({}), and `dev` (`cache:false`, `persistent:true`). ⚠ If installed Turbo is v1, use `pipeline` instead of `tasks`.

2. **`tsconfig.base.json`** per RESEARCH.md §1: `target ES2022`, `module NodeNext`, `moduleResolution NodeNext`, `strict true`, `declaration true`, `esModuleInterop true`, `skipLibCheck true`, `verbatimModuleSyntax true`, `resolveJsonModule true`.

3. **`biome.json`** per RESEARCH.md §7: formatter (2-space) + recommended linter rules; ignore `**/dist`, `**/.next`, `**/generated`; double quotes. Use the schema URL matching the installed Biome major.
  </action>
  <verify>
    <automated>pnpm exec biome check . && node -e "JSON.parse(require('fs').readFileSync('turbo.json','utf8')); JSON.parse(require('fs').readFileSync('tsconfig.base.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f turbo.json && grep -q '"build"' turbo.json` succeeds
    - `grep -q 'NodeNext' tsconfig.base.json` succeeds
    - `grep -q '"strict": true' tsconfig.base.json` succeeds
    - `test -f biome.json` succeeds
    - `pnpm exec biome check .` exits 0
    - `turbo.json` and `tsconfig.base.json` are valid JSON (node parse exits 0)
  </acceptance_criteria>
  <done>
    Turborepo pipeline, shared strict ESM tsconfig, and Biome are configured and pass a clean biome check.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Local docker-compose (Postgres 16 + Redis 7 noeviction) + assert-redis + .gitignore</name>
  <read_first>
    - 01-RESEARCH.md (section 6 — docker-compose.yml + noeviction assertion script)
    - 01-VALIDATION.md (SC1 row + Wave 0 Requirements)
    - .gitignore (current, at repo root — append, do not overwrite existing entries)
  </read_first>
  <action>
1. **`docker-compose.yml`** per RESEARCH.md §6 — `postgres:16` (user/pw/db = omnisync, port 5432, named volume, pg_isready healthcheck) and `redis:7` started with `command: ["redis-server", "--maxmemory-policy", "noeviction"]`, port 6379, redis-cli ping healthcheck. (api/worker services are added by Plan 04 — do not add them here.)

2. **`scripts/assert-redis.ts`** per RESEARCH.md §6 — connect to `process.env.REDIS_URL ?? "redis://localhost:6379"` via ioredis, `config("GET","maxmemory-policy")`, `console.error` + `process.exit(1)` if not `noeviction`, else log OK and `process.exit(0)`. Add a root script `"assert:redis": "tsx scripts/assert-redis.ts"` to package.json.

3. **`.gitignore`** — append (create if missing) entries: `node_modules/`, `dist/`, `.next/`, `.turbo/`, `generated/`, `.env`, `*.log`.
  </action>
  <verify>
    <automated>docker compose config && docker compose up -d && pnpm assert:redis</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'postgres:16' docker-compose.yml` succeeds
    - `grep -q 'noeviction' docker-compose.yml` succeeds
    - `docker compose config` exits 0 (valid compose file)
    - `test -f scripts/assert-redis.ts && grep -q 'maxmemory-policy' scripts/assert-redis.ts` succeeds
    - `grep -q 'assert:redis' package.json` succeeds
    - After `docker compose up -d`, `pnpm assert:redis` exits 0 (Redis is noeviction)
    - `grep -q '.env' .gitignore && grep -q 'node_modules' .gitignore` succeeds
  </acceptance_criteria>
  <done>
    `docker compose up` brings up Postgres + Redis(noeviction); assert:redis confirms the policy (Success Criterion #1). Secrets and build artifacts are gitignored.
  </done>
</task>

</tasks>

<verification>
Run after each task: `pnpm exec biome check .` (where applicable) and the task's automated verify.

Full plan completion:
```bash
pnpm install --frozen-lockfile=false && pnpm exec turbo --version && pnpm exec biome check . && docker compose up -d && pnpm assert:redis
```
Expected: install succeeds, turbo+biome resolve, compose brings up PG+Redis, assert:redis exits 0.
</verification>

<success_criteria>
- Root is an ESM pnpm + Turborepo workspace (package.json, pnpm-workspace.yaml, turbo.json).
- Shared base tsconfig (NodeNext, strict) + Biome configured; `biome check .` clean.
- docker-compose brings up Postgres 16 + Redis 7 with noeviction; assert:redis exits 0 (SC1).
- .gitignore protects secrets and build artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-local-infra/01-01-SUMMARY.md` documenting files created, the installed versions of pnpm/turbo/biome (and any ⚠ VERIFY syntax adjustments made), and confirmation that `docker compose up` + `assert:redis` pass.
</output>
