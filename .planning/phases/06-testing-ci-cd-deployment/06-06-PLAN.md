---
phase: 06-testing-ci-cd-deployment
plan: 06
type: execute
wave: 2
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - apps/worker/tests/integration/idempotency.test.ts
  - README.md
autonomous: false
requirements: [TST-01, TST-03]
user_setup:
  - service: recorded-demo
    why: "OPS-03 requires a recorded walkthrough asset (video/GIF) — capture tooling + human judgment, cannot be automated"
    dashboard_config:
      - task: "Record the demo (per D-03) showing: (1) load-test driving /demo chart, (2) 50->1 dedup result, (3) circuit breaker open/recover under mock-crm fail, (4) kill-PG durability; save as docs/demo.gif (or .mp4) and embed in README"
        location: "Local: cp .env.example .env && pnpm demo, then screen-capture the dashboard"
must_haves:
  truths:
    - "TST-01: `pnpm test -- --coverage` enforces the >=80% line gate on apps/api + apps/worker and fails the run below threshold"
    - "TST-03: the 50-concurrent-identical -> 1-row proof (idempotency.test.ts) is clearly labeled as the TST-03 deliverable and runs under the worker coverage gate"
    - "README documents the one-command demo, the GHCR images, the no-free-always-on-worker deployment decision (D-01), and embeds the recorded walkthrough"
  artifacts:
    - path: "apps/worker/tests/integration/idempotency.test.ts"
      provides: "TST-03 labeled dedup proof"
      contains: "TST-03"
    - path: "README.md"
      provides: "OPS-03 demo doc + deployment decision narrative + GHCR pull instructions"
      contains: "docker compose"
  key_links:
    - from: "README.md"
      to: "scripts/demo.sh / pnpm demo"
      via: "documented one-command demo"
      pattern: "pnpm demo"
    - from: "README.md"
      to: "ghcr.io images"
      via: "documented pull commands"
      pattern: "ghcr.io"
---

<objective>
Final wrap-up of Phase 6. Confirm and DOCUMENT the TST-01 coverage gate (already live — verify it actually fails below 80% and is wired in CI), relabel the existing 50→1 dedup test as the formal TST-03 deliverable, and write the README sections that package the project for recruiters: the one-command demo, GHCR pull instructions, the recorded walkthrough embed, and the informed "no free always-on-worker tier in 2026" deployment decision (D-01).

Purpose: TST-01 and TST-03 are substantively already satisfied (gate set in Phase 1/3, dedup test in Phase 3) — this plan formalizes and labels them, and produces the durable portfolio artifacts (README narrative + recorded demo) that turn the engineering into a credible showcase.
Output: labeled `idempotency.test.ts`, README demo + deployment + GHCR sections, and a recorded-demo checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-testing-ci-cd-deployment/06-CONTEXT.md
@apps/worker/tests/integration/idempotency.test.ts
@apps/api/vitest.config.ts
@apps/worker/vitest.config.ts

<interfaces>
<!-- Concrete contracts the executor needs. -->

Coverage gate (already configured — DO NOT rebuild, only verify + document):
- apps/api/vitest.config.ts: coverage.thresholds = { lines: 80 }, include ["src/**"]
- apps/worker/vitest.config.ts: coverage.thresholds = { lines: 80 }, include ["src/**"]
- CI verify job already runs `pnpm test -- --coverage` (fails non-zero below 80%)
- packages/db + packages/queue have NO thresholds (D-10 — keep it that way)

TST-03 test (apps/worker/tests/integration/idempotency.test.ts, fixed to 5-arg buildProcessor in Plan 06-01):
- describe block "SC-2 / SC-3 idempotent persistence (IDM-02 / IDM-03)"
- it "SC-2: 50 concurrent identical jobs -> exactly 1 events row" — THIS is the TST-03 deliverable

Demo entrypoint (from Plan 06-03): `pnpm demo` -> scripts/demo.sh -> compose up + loadtest
GHCR images (from Plan 06-04): ghcr.io/afrizzal/omnisync-api|worker|mock-crm (:latest, :sha)
Deployment decision (D-01): no free 2026 always-on-worker tier; success = one-command demo + recorded walkthrough + published GHCR images, NOT a live URL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Label idempotency.test.ts as TST-03 and verify the TST-01 coverage gate enforces 80%</name>
  <read_first>
    - apps/worker/tests/integration/idempotency.test.ts (the 5-arg-fixed version from Plan 06-01)
    - apps/api/vitest.config.ts (thresholds: { lines: 80 })
    - apps/worker/vitest.config.ts (thresholds: { lines: 80 })
  </read_first>
  <action>
    1. Relabel the dedup test as the formal TST-03 deliverable WITHOUT changing its logic. In apps/worker/tests/integration/idempotency.test.ts:
       - Update the describe title to: `"TST-03 / SC-2 / SC-3 idempotent persistence (IDM-02 / IDM-03)"`
       - Update the 50-concurrent `it` title to: `"TST-03: 50 concurrent identical jobs -> exactly 1 events row"`
       - Add a top-of-file comment: `// TST-03: concurrent duplicate webhooks result in exactly one stored record (the named, CI-gated proof).`
       Do NOT alter the test bodies or the 5-arg buildProcessor setup.

    2. VERIFY the TST-01 gate actually enforces (do not just trust it). Run the full coverage suite and confirm it passes at >=80%:
       `pnpm test -- --coverage`
       Confirm both apps/api and apps/worker report lines >= 80%. If either app is BELOW 80% after the Phase 6 test additions (the new durability test adds covered lines but no new src; coverage should hold or improve), add targeted unit tests to restore >=80% — but FIRST check the coverage report output to see which app and which files dropped. Do not add tests speculatively.

    3. Conform to Biome: `pnpm --filter @omnisync/worker exec biome check --write tests/integration/idempotency.test.ts`.
  </action>
  <verify>
    <automated>pnpm test -- --coverage</automated>
  </verify>
  <acceptance_criteria>
    - `idempotency.test.ts` describe title contains `TST-03`
    - `idempotency.test.ts` the 50-concurrent it title contains `TST-03:`
    - `idempotency.test.ts` still calls `buildProcessor(` with 5 arguments (unchanged from Plan 06-01)
    - `pnpm test -- --coverage` exits 0 with apps/api AND apps/worker each at >=80% line coverage
  </acceptance_criteria>
  <done>TST-03 is explicitly labeled and CI-gated; TST-01's 80% gate is verified to pass and enforce.</done>
</task>

<task type="auto">
  <name>Task 2: Write the README demo, GHCR, and deployment-decision sections (OPS-03)</name>
  <read_first>
    - README.md (current state — append/extend, do not destroy existing content; read it fully first)
    - .planning/phases/06-testing-ci-cd-deployment/06-CONTEXT.md (D-01 deployment reframe, D-03 recorded-demo contents)
    - .planning/PROJECT.md (core value statement to echo)
    - scripts/demo.sh (the exact one-command flow to document — from Plan 06-03)
  </read_first>
  <action>
    Add (or update if present) these README sections. If README.md does not exist, create it with a project title + the core-value one-liner from PROJECT.md, then these sections:

    1. `## Quick Demo (one command)`:
       ```
       Requires Docker + Node 22 + pnpm.

       cp .env.example .env
       pnpm install
       pnpm demo
       ```
       Explain: brings up the full stack (api, worker, postgres, redis, mock-crm, dashboard), then runs the multi-channel autocannon load test. Open http://localhost:3000/demo to watch events flow live. Stop with `docker compose down -v`.

    2. `## Recorded Walkthrough`:
       Embed the demo asset: `![OmniSync demo](docs/demo.gif)` (the asset is produced in the checkpoint below). Caption it with the four scenes from D-03: (1) load-test driving the /demo chart, (2) 50→1 concurrent-dedup, (3) circuit breaker opening/recovering under mock-crm failure, (4) kill-Postgres durability (pause PG, events stay queued, unpause, they drain). Add a placeholder note if the asset is not yet recorded: `> Recording: see docs/demo.gif (capture via pnpm demo).`

    3. `## Container Images (GHCR)`:
       Document the published images and pull commands:
       ```
       docker pull ghcr.io/afrizzal/omnisync-api:latest
       docker pull ghcr.io/afrizzal/omnisync-worker:latest
       docker pull ghcr.io/afrizzal/omnisync-mock-crm:latest
       ```
       Note: images are built+pushed by CI on merge to master (build-only on PRs).

    4. `## Deployment Decision: Why No Live Public URL` (the D-01 interview talking point):
       Explain in 3-4 sentences: as of 2026 there is no $0 always-on background-worker tier (Render workers are paid, Fly.io free tier is gone, Railway/Koyeb insufficient or scale-to-zero). A BullMQ worker that sleeps breaks the always-on guarantee. So OmniSync ships as published GHCR images + a one-command reproducible demo + a recorded walkthrough — a more reliable recruiter artifact than a free-tier URL that may be cold or down. The compose file + images make a live deploy a drop-in later (Oracle Cloud Always Free or ~$2-5/mo Fly.io) if desired.

    5. `## Testing` (brief): list the signature tests — TST-02 kill-Postgres durability (Testcontainers), TST-03 50→1 concurrent dedup, TST-04 Playwright DLQ re-queue E2E, and the >=80% coverage gate (TST-01). Commands: `pnpm test -- --coverage` and `pnpm exec playwright test` (needs the stack up).

    Conform to Biome/markdown formatting if applicable.
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('README.md','utf8'); for(const s of ['pnpm demo','ghcr.io/afrizzal/omnisync-api','docker compose','TST-02']){ if(!r.includes(s)) throw new Error('README missing: '+s); } console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains a one-command demo section with `cp .env.example .env` and `pnpm demo`
    - `README.md` contains `ghcr.io/afrizzal/omnisync-api`, `omnisync-worker`, and `omnisync-mock-crm` pull commands
    - `README.md` contains a deployment-decision section explaining no free always-on-worker tier
    - `README.md` references the recorded walkthrough asset path (e.g. `docs/demo.gif`)
    - `README.md` lists TST-02, TST-03, TST-04, and the coverage gate
  </acceptance_criteria>
  <done>README packages the project: one-command demo, GHCR images, the informed deployment decision, and the testing story.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Record the demo walkthrough (manual, OPS-03 / D-03)</name>
  <what-built>The full demo pipeline (compose stack + load-test + dashboard) and README documentation are complete. The recorded walkthrough asset (OPS-03 / D-03) requires human screen-capture and judgment.</what-built>
  <how-to-verify>
    1. Run the one-command demo locally: `cp .env.example .env && pnpm install && pnpm demo`.
    2. Confirm the dashboard at http://localhost:3000/demo shows events flowing as the load test runs.
    3. Record a short walkthrough (GIF or MP4) showing the four D-03 scenes IN ORDER:
       (1) the load-test driving the /demo chart,
       (2) the 50->1 concurrent-dedup result (run `pnpm --filter @omnisync/worker test -- tests/integration/idempotency.test.ts` or show the dashboard),
       (3) the circuit breaker opening/recovering — toggle mock-crm: `curl -X POST http://localhost:3002/admin/failure-mode -H 'content-type: application/json' -d '{"mode":"fail","rate":1}'`, watch the DLQ fill, then set rate back to 0,
       (4) the kill-Postgres durability scenario — `docker pause <postgres-container>`, observe events stay queued, `docker unpause`, observe them drain.
    4. Save the asset to `docs/demo.gif` (or .mp4 + update the README embed) and confirm it renders in README.md.
    5. Confirm GHCR package visibility is Public and branch protection requires the `verify` check (from Plan 06-04 user_setup).
  </how-to-verify>
  <action>This is a manual capture gate — no code the executor can write. The executor pauses so the user records the demo walkthrough (the four D-03 scenes), saves it to docs/demo.gif (or .mp4), confirms it renders in the README, and completes the GHCR-public + branch-protection settings.</action>
  <verify>Manual — the docs/demo.gif (or .mp4) asset exists and renders in README.md; the dashboard at http://localhost:3000/demo showed live events during the recording.</verify>
  <done>The recorded walkthrough asset is committed under docs/ and embedded in the README; GHCR images are public and branch protection is on.</done>
  <resume-signal>Type "approved" once the recorded demo asset is in docs/ and embedded in the README, or describe what is blocking the recording.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm test -- --coverage` exits 0 with apps/api + apps/worker each >=80% (TST-01 enforced)
- idempotency.test.ts is labeled TST-03 and runs under the worker gate
- README documents demo, GHCR images, deployment decision, and testing story
- Recorded walkthrough asset committed and embedded (checkpoint)
</verification>

<success_criteria>
- TST-01: coverage gate verified to enforce >=80% and fail below it, documented in README
- TST-03: 50->1 dedup proof formally labeled and CI-gated
- OPS-03: one-command demo + recorded walkthrough + GHCR images all documented and present (the recruiter-facing package)
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-ci-cd-deployment/06-06-SUMMARY.md`
</output>
